<h1>Lista de itens</h1>

<p>Se algum dos itens abaixo lhe interessar, por favor, mande um email para <%= mail_to "hugomari@gmail.com" %> com seu nome e telefone para que possamos combinar o pagamento e retirada.</p>

<table width="100%">
<% @products.each_slice(5) do |product_row| %>
  <tr>
    <% product_row.each do |product| %>
      <td class="product">
        <h5><%= link_to "#{product._id}. #{product.name} - #{to_currency(product.price)}", product_path(product) %></h5>
        <% product.photos[0...1].each do |photo| %>
          <%= image_tag photo.image.url(:thumb) %>
        <% end %>
      </td>
    <% end %>
  </tr>
<% end %>
</table>

<br />
